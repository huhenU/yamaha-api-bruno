meta {
  name: setIrSensor
  type: http
  seq: 23
}

get {
  url: {{BaseURL}}/v1/system/setIrSensor?enable=true
  body: none
  auth: none
}

params:query {
  enable: true
}

docs {
  For setting remote control IR sensor
  
  **Request Parameters**
  | name | type | required | description |
  |------|--------|----------|-----------|
  | enable | boolean | yes | Specifies setting of IR sensor
}
